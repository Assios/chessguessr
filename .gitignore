node_modules

# Ignore build outputs and API build folder (api/* so !api/og.ts can re-include)
api/*
!api/og.ts

.cache
.env
.vercel
.output
.vscode

/build/
/public/build
#/api files are now ignored by directory rule above

app/tailwind.css
